# homebrew-tap

Homebrew tap for [exifregistry](https://exifregistry.com), the photographer's metadata toolkit.

```bash
brew install daviarndt/tap/exifregistry
```

The formula is updated automatically when a new version is published to npm.
